name: Deploy Lúmina

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Copiar .env.local
        run: cp ~/.env.local .env.local

      - name: Instalar dependencias
        run: npm ci

      - name: Build
        run: npm run build

      - name: Reiniciar con PM2
        run: sudo pm2 restart lumina
